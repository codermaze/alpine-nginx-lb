upstream sample-app-engine-1 {
  server sample-app-engine-1:3100;
}

upstream sample-app-engine-2 {
  server sample-app-engine-2:3100;
}

upstream sample-app-engine-3 {
  server sample-app-engine-3:3100;
}



server {
  root /var/www;
  index index.html index.htm;

  # Make site accessible from http://localhost/
  # All QA1 Sites are available thru port 8881
  # listen 8881;
  server_name qa1.test.com;

  add_header Access-Control-Allow-Origin '';

  location /ui/app/firstapp {
    proxy_pass http://sample-app-engine-1;
  }

#  location /ui/app/secondapp {
#    proxy_pass http://sample-app-engine-2;
#  }

#  location /ui/app/thirdapp {
#    proxy_pass http://sample-app-engine-3;
#  }

  # If nginx can't find a file, fallback to the homepage.
  location @root {
    rewrite .* / redirect;
  }
}

server {
  root /var/www;
  index index.html index.htm;

  # Make site accessible from http://localhost/
  # All QA2 sites available thru Port 8882
  # listen 8882;
  server_name qa2.test.com;

  add_header Access-Control-Allow-Origin '';

  location /ui/app/secondapp {
    proxy_pass http://sample-app-engine-2;
  }

  # If nginx can't find a file, fallback to the homepage.
  location @root {
    rewrite .* / redirect;
  }
}

server {
  root /var/www;
  index index.html index.htm;

  # Make site accessible from http://localhost/
  # All QA2 internal sites available thru port 8883 
  # listen 8883;
  server_name qa2.internal.test.com;

  add_header Access-Control-Allow-Origin '';

  location /ui/app/thirdapp {
    proxy_pass http://sample-app-engine-3;
  }

  # If nginx can't find a file, fallback to the homepage.
  location @root {
    rewrite .* / redirect;
  }
}

# server {
#   root /var/www;
#   index index.html index.htm;

#   # Make site accessible from http://localhost/
#   # NO RESTRICTIONS ON PORT 8000
#   # listen 8000;
#   server_name qa1a.test.com;

#   add_header Access-Control-Allow-Origin '';

#  location /ui/app/firstapp {
#    proxy_pass http://sample-app-engine-1;
#  }

#  location /ui/app/secondapp {
#    proxy_pass http://sample-app-engine-2;
#  }

#  location /ui/app/thirdapp {
#    proxy_pass http://sample-app-engine-3;
#  }

#   # If nginx can't find a file, fallback to the homepage.
#   location @root {
#     rewrite .* / redirect;
#   }
# }
